server:
  port: 9001

spring:
  application:
    name: component-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saiyizero/myspringcloud-config
          search-paths: myaccountsystem-config
          username: saiyizero
          password: sa26212835

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8661/eureka/